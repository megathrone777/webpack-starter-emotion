export interface TColors {
  orange: string;
  tomato: string;
}

export interface TDevices {
  desktop: string;
  tablet: string;
  mobile: string;
}

export interface TFonts {
  initialFontSize: number;
  normal: number;
}
